[pytest]
# Only collect our smoke tests. The vendored tool sources (scons/, nDPI/, pkt2flow/)
# carry their own test_*.py files that need SCons' test harness and must not be collected.
testpaths = tests
norecursedirs = .venv scons nDPI pkt2flow data results results_synthetic helper-code-files .git
